name: 'proskiro'
version: '1.0.0'
config-version: 2

profile: 'proskiro'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  proskiro:
    staging:
      +materialized: view
    marts:
      +materialized: view